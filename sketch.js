// ----------------------- starts from 3/16/2022 ------------- // 
var backgroundImage;
var redGhost, greenGhost, yellowGhost
var player;
function preload() {
  backgroundImage = loadImage("assets/ground.png");


}

function setup() {
  canvas = createCanvas(windowWidth, windowHeight);

}

function draw() {
  background(backgroundImage);

}
